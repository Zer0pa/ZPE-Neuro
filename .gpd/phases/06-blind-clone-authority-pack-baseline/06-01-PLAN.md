---
phase: 06-blind-clone-authority-pack-baseline
plan: 01
type: execute
wave: 1
depends_on: [05]
files_modified:
  - pyproject.toml
  - src/zpe_neuro/ibl_public.py
  - src/zpe_neuro/wave1.py
  - .gpd/phases/06-blind-clone-authority-pack-baseline/06-01-PLAN.md
  - .gpd/phases/06-blind-clone-authority-pack-baseline/06-01-SUMMARY.md
  - .gpd/ROADMAP.md
  - .gpd/STATE.md
  - .gpd/state.json
  - runbooks/<timestamp>_codex_receipt.md
interactive: false

conventions:
  units: "microvolts, samples, seconds, hertz"
  framing: "clean-env gate baseline; blind-clone closure remains a separate gate"
  artifact_policy: "Gate replays must treat shared artifact roots as mutable runtime surfaces, not as proof that parallel blind-clone execution is already safe"

approximations:
  - name: "Sequential clean-env gate baseline"
    parameter: "fresh `.[gate,dev]` environment with sequential gate writes into the repo-local artifact root"
    validity: "Phase 6 establishes a minimal reproducibility baseline for the unit slice plus Gate C and Gate D only"
    breaks_when: "the sequential baseline is narrated as full blind-clone closure or as evidence that heavier proof/public replay already works cleanly"
    check: "keep the shared artifact-root collision explicit and require isolated outputs for stronger cold-clone claims"

contract:
  schema_version: 1
  scope:
    question: "Can the repo define and validate a minimal clean-env Gate C/D authority-pack baseline without relying on hidden workspace dependencies?"
  claims:
    - id: claim-clean-env-gate-baseline
      statement: "The repo declares a minimal gate-validation dependency surface and that surface passes the phase unit slice plus Gate C and Gate D in a fresh environment."
      deliverables: [deliv-gate-extra, deliv-clean-gate-artifacts, deliv-phase-summary]
      acceptance_tests: [test-gate-extra, test-clean-env-replay]
      references: [ref-phase5, ref-repo-gates]
    - id: claim-blind-clone-gap-visibility
      statement: "The remaining gap between a sequential clean-env baseline and a true blind-clone authority pack is explicit rather than narrated away."
      deliverables: [deliv-phase-summary]
      acceptance_tests: [test-gap-visibility]
      references: [ref-phase5, ref-repo-gates]
  deliverables:
    - id: deliv-gate-extra
      kind: config
      path: pyproject.toml
      description: "Minimal clean-env gate dependency surface for the authority-pack baseline"
      must_contain: [gate extra, scikit-learn dependency, spikeinterface dependency, pynwb dependency]
    - id: deliv-clean-gate-artifacts
      kind: report
      path: artifacts/2026-02-20_zpe_neuro_wave1/{gate_c_summary.json,gate_d_summary.json}
      description: "Sequential fresh-env Gate C and Gate D validation results"
      must_contain: [Gate C PASS, Gate D PASS]
    - id: deliv-phase-summary
      kind: note
      path: .gpd/phases/06-blind-clone-authority-pack-baseline/06-01-SUMMARY.md
      description: "Phase 6 summary of the clean-env baseline, dependency repairs, and remaining blind-clone gap"
      must_contain: [clean-env baseline, hidden dependency repair, remaining blind-clone gap]
  references:
    - id: ref-phase5
      kind: prior_artifact
      locator: .gpd/phases/05-ibl-breadth-closure-or-falsification/05-01-SUMMARY.md
      role: prior_gate
      why_it_matters: "Phase 5 closed public breadth and hands Phase 6 the next unresolved blind-clone-facing gate"
      applies_to: [claim-clean-env-gate-baseline, claim-blind-clone-gap-visibility]
      must_surface: true
      required_actions: [read, carry_forward]
    - id: ref-repo-gates
      kind: prior_artifact
      locator: proofs/selected_artifacts/2026-03-20_zpe_neuro_repo_realignment/{gate_c_summary.json,gate_d_summary.json}
      role: baseline
      why_it_matters: "The repo-local March 20 reruns define the gate behavior that the fresh-env baseline must reproduce without hidden workspace assumptions"
      applies_to: [claim-clean-env-gate-baseline]
      must_surface: true
      required_actions: [read, compare]
  acceptance_tests:
    - id: test-gate-extra
      subject: claim-clean-env-gate-baseline
      kind: configuration
      procedure: "Inspect pyproject.toml and verify that the minimal gate-validation extra declares the dependencies actually exercised by Gate C."
      pass_condition: "A dedicated gate extra exists and includes the Gate C/Gate D clean-env surface."
      evidence_required: [deliv-gate-extra]
      automation: automated
    - id: test-clean-env-replay
      subject: claim-clean-env-gate-baseline
      kind: execution
      procedure: "Install `.[gate,dev]` into a fresh venv, run the Phase 1-5 unit slice, then run Gate C and Gate D sequentially."
      pass_condition: "The clean-env unit slice plus Gate C and Gate D all pass without ambient workspace dependencies."
      evidence_required: [deliv-clean-gate-artifacts, deliv-phase-summary]
      automation: automated
    - id: test-gap-visibility
      subject: claim-blind-clone-gap-visibility
      kind: consistency
      procedure: "Inspect the phase summary for explicit remaining gaps between sequential clean-env replay and a true blind-clone authority pack."
      pass_condition: "The summary keeps shared artifact-root collisions, full cold-clone replay, and release-boundary gaps explicit."
      evidence_required: [deliv-phase-summary]
      automation: automated
  forbidden_proxies:
    - id: fp-ambient-env-pass
      subject: claim-clean-env-gate-baseline
      proxy: "Treating the shared workspace environment as evidence that the repo is clean-env reproducible"
      reason: "Phase 6 exists to remove hidden dependency assumptions, not preserve them."
    - id: fp-sequential-is-blind-clone
      subject: claim-blind-clone-gap-visibility
      proxy: "Treating sequential fresh-env gate replay as full blind-clone closure"
      reason: "The current phase only establishes the baseline and surfaces the remaining cold-clone and artifact-isolation work."
---

<objective>
Execute the first honest blind-clone-adjacent follow-up after public breadth closure.

Purpose: repair hidden clean-env dependency assumptions, establish a minimal `.[gate,dev]` baseline for the repo-local gate slice, and leave the remaining blind-clone gap explicit.
Output: clean-env package boundary, repaired Gate C spikeinterface probe, fresh-env gate evidence, and updated operational state.
</objective>
