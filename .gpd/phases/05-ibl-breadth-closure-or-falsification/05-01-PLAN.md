---
phase: 05-ibl-breadth-closure-or-falsification
plan: 01
type: execute
wave: 1
depends_on: [04]
files_modified:
  - src/zpe_neuro/ibl_public.py
  - src/zpe_neuro/ibl_refinement.py
  - src/zpe_neuro/wave1.py
  - tools/run_ibl_public_waveform_eval.py
  - tools/run_ibl_bounded_refinement.py
  - tests/test_ibl_refinement.py
  - proofs/selected_artifacts/<run>/ibl_refinement_search.json
  - proofs/selected_artifacts/<run>/public_corpus_ibl_waveform_eval.json
  - proofs/selected_artifacts/<run>/ibl_refinement_decision.md
  - proofs/selected_artifacts/<run>/public_corpus_summary.json
  - .gpd/STATE.md
  - .gpd/state.json
  - .gpd/ROADMAP.md
  - ROADMAP.md
  - PUBLIC_AUDIT_LIMITS.md
interactive: false

conventions:
  units: "microvolts, samples, seconds, hertz"
  coordinates: "zero-based sample windows with half-open ranges [start, start + sample_limit)"
  framing: "bounded IBL rescue or falsification under the unchanged downstream contract"

approximations:
  - name: "Bounded IBL search"
    parameter: "coarse chunk grid, fixed 8-channel windows, deterministic representative windows"
    validity: "Phase 5 only decides whether bounded local refinement can rescue the counted extracellular breadth target"
    breaks_when: "the phase starts treating bounded search as exhaustive proof over the entire IBL recording"
    check: "all conclusions must remain scoped to the bounded search policy and the unchanged downstream contract"

contract:
  schema_version: 1
  scope:
    question: "Can bounded IBL refinement close the counted extracellular breadth target without weakening the current downstream contract?"
  claims:
    - id: claim-ibl-bounded-search
      statement: "The repo contains an explicit bounded IBL search policy and result that either finds a passing second extracellular target or falsifies that rescue route more strongly."
      deliverables: [deliv-search-summary, deliv-ibl-eval, deliv-decision-note]
      acceptance_tests: [test-search-summary, test-ibl-eval]
      references: [ref-ibl-fail, ref-dandi, ref-way-forward]
    - id: claim-breadth-rerun
      statement: "The breadth summary is rerun from the new IBL result without proxy logic."
      deliverables: [deliv-public-summary]
      acceptance_tests: [test-public-summary]
      references: [ref-ibl-fail, ref-breadth-summary]
  deliverables:
    - id: deliv-search-summary
      kind: report
      path: proofs/selected_artifacts/<run>/ibl_refinement_search.json
      description: "Bounded IBL search summary with chunk, channel-window, shortlist, and cleanup details"
      must_contain: [search_config, top_coarse_candidates, evaluated_candidates, best_candidate, cleanup]
    - id: deliv-ibl-eval
      kind: report
      path: proofs/selected_artifacts/<run>/public_corpus_ibl_waveform_eval.json
      description: "Best bounded-search IBL evaluation under the unchanged downstream contract"
      must_contain: [search_candidate, codec_metrics, nwb_roundtrip, spikeinterface, evaluation_status]
    - id: deliv-decision-note
      kind: note
      path: proofs/selected_artifacts/<run>/ibl_refinement_decision.md
      description: "Decision note explaining whether bounded IBL rescue succeeded or failed"
      must_contain: [best candidate, interpretation, next step]
    - id: deliv-public-summary
      kind: report
      path: proofs/selected_artifacts/<run>/public_corpus_summary.json
      description: "Breadth rerun summary grounded in the updated IBL artifact"
      must_contain: [per-target verdicts, IBL execution status, breadth verdict]
  references:
    - id: ref-ibl-fail
      kind: prior_artifact
      locator: proofs/selected_artifacts/2026-03-20_zpe_neuro_ibl_waveform_probe/public_corpus_ibl_waveform_eval.json
      role: starting_point
      why_it_matters: "Defines the narrow IBL failure surface that Phase 5 must either rescue or falsify more strongly"
      applies_to: [claim-ibl-bounded-search, claim-breadth-rerun]
      must_surface: true
      required_actions: [read, compare]
    - id: ref-dandi
      kind: prior_artifact
      locator: proofs/selected_artifacts/2026-03-20_zpe_neuro_repo_realignment/public_corpus_eval_dandi_000034_mouse412804_ecephys.json
      role: authority_anchor
      why_it_matters: "Prevents Phase 5 from using the positive DANDI anchor as a breadth proxy"
      applies_to: [claim-ibl-bounded-search]
      must_surface: true
      required_actions: [read, preserve]
    - id: ref-breadth-summary
      kind: prior_artifact
      locator: proofs/selected_artifacts/2026-03-21_zpe_neuro_breadth_adjudication/public_corpus_summary.json
      role: current_verdict
      why_it_matters: "Defines the exact breadth state that the new IBL result can change or strengthen"
      applies_to: [claim-breadth-rerun]
      must_surface: true
      required_actions: [read, compare]
    - id: ref-way-forward
      kind: prior_artifact
      locator: docs/team_packet/07_WAY_FORWARD.md
      role: decision_frame
      why_it_matters: "Keeps the lane fork honest: either rescue the second extracellular target or move to explicit fail-forward"
      applies_to: [claim-ibl-bounded-search]
      must_surface: true
      required_actions: [read, use]
  acceptance_tests:
    - id: test-search-summary
      subject: claim-ibl-bounded-search
      kind: consistency
      procedure: "Inspect the bounded IBL search summary for deterministic search policy, shortlist ranking, final candidate choice, and temp-cache cleanup."
      pass_condition: "The search policy is explicit, bounded, reproducible, and does not leave temp artifacts as hidden proof."
      evidence_required: [deliv-search-summary, deliv-decision-note]
      automation: automated
    - id: test-ibl-eval
      subject: claim-ibl-bounded-search
      kind: execution
      procedure: "Run the best bounded-search candidate through codec metrics, NWB roundtrip, and SpikeInterface sorter probe."
      pass_condition: "The IBL artifact shows either a real passing second target or a stronger falsification of the bounded rescue route."
      evidence_required: [deliv-ibl-eval, deliv-decision-note, ref-ibl-fail]
      automation: automated
    - id: test-public-summary
      subject: claim-breadth-rerun
      kind: consistency
      procedure: "Rerun breadth adjudication using the new IBL artifact and inspect the summary for explicit DANDI, AJILE, and IBL accounting."
      pass_condition: "The breadth summary uses the new IBL evidence without proxy promotion."
      evidence_required: [deliv-public-summary, deliv-ibl-eval, ref-breadth-summary]
      automation: automated
  forbidden_proxies:
    - id: fp-dandi-breadth
      subject: claim-breadth-rerun
      proxy: "Using the DANDI Tier 1 pass to imply breadth closure if the new IBL result still fails"
      reason: "DANDI remains sovereign as an anchor but singular as breadth evidence."
    - id: fp-threshold-drift
      subject: claim-ibl-bounded-search
      proxy: "Lowering downstream evaluation requirements just to force a passing IBL candidate"
      reason: "Phase 5 may search chunk, channel, and representative slice choice, but it may not quietly redefine the contract."
    - id: fp-temp-artifact-proof
      subject: claim-ibl-bounded-search
      proxy: "Leaving large temporary chunk caches behind and treating them as part of the authoritative proof surface"
      reason: "Only explicit proof artifacts and logged evidence should survive the run."
---

<objective>
Execute the decisive IBL follow-up without weakening the sovereign gate.

Purpose: run a bounded IBL rescue-or-falsify search under the existing downstream contract, rerun breadth adjudication from the new result, and update lane state surfaces honestly.
Output: bounded-search artifacts, updated IBL eval, rerun breadth summary, and state/roadmap updates.
</objective>
