---
phase: 01-public-corpus-window-policy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/zpe_neuro/public_corpus.py
  - tools/run_public_corpus_eval.py
  - tests/test_public_corpus_window_selection.py
interactive: false

conventions:
  units: "microvolts, samples, seconds, hertz"
  metric: "N/A"
  coordinates: "zero-based sample windows with half-open ranges [start, start + sample_limit)"

approximations:
  - name: "Small candidate set"
    parameter: "candidate_window_count"
    validity: "M1-local reruns where streaming a few short slices is cheap"
    breaks_when: "selection requires dense whole-file scanning or large local caches"
    check: "selection artifact must record candidate starts and scores so under-sampling is auditable"

contract:
  schema_version: 1
  scope:
    question: "Can the public harness replace fixed first-window probing with reproducible candidate-window selection without regressing the DANDI Tier 1 anchor?"
  claims:
    - id: claim-window-policy
      statement: "The public-corpus harness uses deterministic candidate-window selection and surfaces the chosen slice explicitly."
      deliverables: [deliv-window-selection, deliv-rerun-summary]
      acceptance_tests: [test-window-selection, test-dandi-anchor]
      references: [ref-dandi, ref-ajile, ref-way-forward]
  deliverables:
    - id: deliv-window-selection
      kind: dataset
      path: proofs/selected_artifacts/<run>/public_corpus_window_selection_summary.json
      description: "Window-selection summary with candidate starts, scores, selected windows, and rejection notes"
      must_contain: [candidate starts, selected window, quiet-window notes]
    - id: deliv-rerun-summary
      kind: report
      path: proofs/selected_artifacts/<run>/public_corpus_summary.json
      description: "Rerun summary produced under the selected-window policy"
      must_contain: [per-target verdicts, selected window metadata]
  references:
    - id: ref-dandi
      kind: dataset
      locator: proofs/selected_artifacts/2026-03-20_zpe_neuro_repo_realignment/public_corpus_eval_dandi_000034_mouse412804_ecephys.json
      role: benchmark
      why_it_matters: "Current positive public anchor that must not regress"
      applies_to: [claim-window-policy]
      must_surface: true
      required_actions: [read, compare]
    - id: ref-ajile
      kind: dataset
      locator: proofs/selected_artifacts/2026-03-20_zpe_neuro_repo_realignment/public_corpus_eval_ajile12_sub01_ses7_ecephys.json
      role: benchmark
      why_it_matters: "Current breadth-fail artifact that motivates replacing fixed-window probing"
      applies_to: [claim-window-policy]
      must_surface: true
      required_actions: [read, compare]
    - id: ref-way-forward
      kind: prior_artifact
      locator: docs/team_packet/07_WAY_FORWARD.md
      role: definition
      why_it_matters: "Calls for candidate scanning, representative slice selection, and explicit rejection of quiet-slice wins"
      applies_to: [claim-window-policy]
      must_surface: true
      required_actions: [read, use]
  acceptance_tests:
    - id: test-window-selection
      subject: claim-window-policy
      kind: reproducibility
      procedure: "Run the public corpus harness with candidate scanning and inspect the selection artifact for deterministic starts, candidate metrics, and a recorded selected window per target."
      pass_condition: "The harness no longer defaults to a fixed first-window policy and the selection artifact exists."
      evidence_required: [deliv-window-selection, ref-dandi, ref-ajile, ref-way-forward]
      automation: hybrid
    - id: test-dandi-anchor
      subject: claim-window-policy
      kind: consistency
      procedure: "Rerun DANDI under the selected-window policy and compare the verdict against the March 20 anchor."
      pass_condition: "The DANDI target remains PASS under the new policy."
      evidence_required: [deliv-rerun-summary, ref-dandi]
      automation: automated
  forbidden_proxies:
    - id: fp-quiet-compression
      subject: claim-window-policy
      proxy: "Selecting or accepting a window because compression is high while codec eventfulness remains absent"
      reason: "Would recreate the AJILE false-positive pattern"
    - id: fp-dandi-breadth
      subject: claim-window-policy
      proxy: "Treating a preserved DANDI pass as breadth closure"
      reason: "Phase 1 only improves truthfulness of slice selection"
  uncertainty_markers:
    weakest_anchors: ["Whether simple candidate metrics capture informative slices without favoring noise"]
    disconfirming_observations: ["DANDI regresses or selection still picks obviously quiet windows"]
---

<objective>
Implement deterministic candidate-window selection for the public corpus harness and rerun DANDI plus AJILE under the new policy.

Purpose: make the public breadth evidence more trustworthy without changing the governing acceptance gate.
Output: code changes, a test, window-selection artifacts, and a rerun public summary.
</objective>

<execution_context>
@/Users/prinivenpillay/.codex/get-physics-done/workflows/execute-plan.md
@/Users/prinivenpillay/.codex/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/01-public-corpus-window-policy/01-CONTEXT.md
@.gpd/phases/01-public-corpus-window-policy/01-RESEARCH.md
@proofs/selected_artifacts/2026-03-20_zpe_neuro_repo_realignment/public_corpus_eval_dandi_000034_mouse412804_ecephys.json
@proofs/selected_artifacts/2026-03-20_zpe_neuro_repo_realignment/public_corpus_eval_ajile12_sub01_ses7_ecephys.json
@docs/team_packet/07_WAY_FORWARD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic candidate-window selection</name>
  <files>src/zpe_neuro/public_corpus.py</files>
  <action>Add start-offset support, candidate-window generation, scoring metrics, and selection-artifact writing to the public corpus harness.</action>
  <verify>Check that candidate starts are deterministic, recorded, and compatible with streamed slicing.</verify>
  <done>A selection artifact exists for each public target and the harness can evaluate a selected window instead of the first window.</done>
</task>

<task type="auto">
  <name>Task 2: Expose and test the new policy</name>
  <files>tools/run_public_corpus_eval.py, tests/test_public_corpus_window_selection.py</files>
  <action>Add CLI flags for the candidate-window policy and create a focused test covering deterministic selection and quiet-window rejection behavior.</action>
  <verify>Run the targeted test file locally and confirm the CLI still accepts the default public-corpus rerun path.</verify>
  <done>The new policy is exercised by at least one local automated test.</done>
</task>

<task type="auto">
  <name>Task 3: Rerun DANDI and AJILE with the selected-window policy</name>
  <files>proofs/selected_artifacts/&lt;run&gt;/public_corpus_window_selection_summary.json, proofs/selected_artifacts/&lt;run&gt;/public_corpus_summary.json</files>
  <action>Execute the harness on local hardware, capture the new selection artifacts, and inspect whether DANDI remains stable and whether AJILE changes materially under informative-window selection.</action>
  <verify>DANDI must remain PASS and the artifacts must explicitly reject quiet-window false progress.</verify>
  <done>A new rerun summary and selection artifact exist under the repo-local proof surface.</done>
</task>

</tasks>

<verification>
Confirm the selected-window policy no longer uses the first window implicitly, that DANDI remains PASS, and that any AJILE outcome is represented through eventfulness-aware evidence rather than compression alone.
</verification>

<success_criteria>
Deterministic candidate-window selection is implemented, selection artifacts exist, DANDI remains PASS, and the rerun public summary is more decision-grade than the fixed-window baseline.
</success_criteria>

<output>
After completion, create `.gpd/phases/01-public-corpus-window-policy/01-01-SUMMARY.md`.
</output>
