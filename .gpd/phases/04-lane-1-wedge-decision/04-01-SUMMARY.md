---
phase: 04-lane-1-wedge-decision
plan: 01
depth: full
one-liner: "Phase 4 locks Lane 1 to a narrower extracellular authority wedge, keeps IBL as an explicit breadth blocker, and pushes broader neural coverage into a second-mode lane."
subsystem: [strategy, evidence, product]
tags: [wedge, fail-forward, extracellular, scope]
requires: [03-02-public-breadth-rerun-summary]
provides:
  - a Lane 1 wedge recommendation memo
  - a residual decision register
  - updated lane state surfaces
affects: [04-lane-1-wedge-decision, lane-1-authority, commercialization-boundary]
completed: 2026-03-21
---

# Phase 04 Plan 01: Lane 1 Wedge Decision

**Phase 4 converts the fail-forward breadth verdict into an explicit first-lane decision: keep Lane 1 narrow, extracellular, and authority-first.**

## Key Results

- Produced `proofs/selected_artifacts/2026-03-21_zpe_neuro_lane1_wedge_decision/lane_1_wedge_recommendation.md`.
- Produced `proofs/selected_artifacts/2026-03-21_zpe_neuro_lane1_wedge_decision/lane_1_residual_decision_register.json`.
- Updated `.gpd/STATE.md`, `.gpd/state.json`, `.gpd/ROADMAP.md`, `ROADMAP.md`, and `PUBLIC_AUDIT_LIMITS.md` so the wedge decision is part of the authoritative lane state.

## Wedge Decision

- Lane 1 is now explicitly an extracellular research-infrastructure wedge anchored on the DANDI `000034` public pass.
- AJILE12 remains `OUT_OF_FAMILY` for the current spike-oriented codec and does not count as hidden near-term breadth.
- IBL remains the counted extracellular breadth blocker because waveform execution is real but downstream evaluation is still `FAIL`.

## Residual Decisions

- A bounded IBL refinement pass is optional strengthening work, not a retroactive breadth pass and not a prerequisite for the current scope lock.
- Broader human or intracranial coverage requires a second representation mode or a separate lane.
- Commercialization, release readiness, and blind-clone claims remain blocked.

## Next Task

Start Phase 4.5 fail-forward follow-up: choose whether the highest-leverage next step is a bounded local IBL downstream refinement or an explicit second-mode lane definition. Neither path should dilute the DANDI anchor or hide the open breadth blocker.
