---
phase: 04-lane-1-wedge-decision
plan: 01
type: execute
wave: 1
depends_on: [03]
files_modified:
  - proofs/selected_artifacts/<run>/lane_1_wedge_recommendation.md
  - proofs/selected_artifacts/<run>/lane_1_residual_decision_register.json
  - .gpd/STATE.md
  - .gpd/state.json
  - ROADMAP.md
  - PUBLIC_AUDIT_LIMITS.md
interactive: false

conventions:
  units: "microvolts, samples, seconds, hertz"
  coordinates: "zero-based sample windows with half-open ranges [start, start + sample_limit)"
  framing: "authority-first wedge decision with explicit fail-forward residuals"

approximations:
  - name: "Evidence-bound Lane 1 recommendation"
    parameter: "current repo-local DANDI, AJILE, and IBL packet only"
    validity: "Phase 4 decides the first lane from already executed public evidence"
    breaks_when: "the wedge recommendation starts implying new corpus success, release readiness, or commercialization closure"
    check: "every recommendation statement must trace back to carried-forward proof artifacts and explicit residual risks"

contract:
  schema_version: 1
  scope:
    question: "What is the strongest honest Lane 1 wedge recommendation after the explicit breadth fail-forward outcome?"
  claims:
    - id: claim-lane-decision
      statement: "The repo contains an explicit Lane 1 wedge recommendation that matches the current evidence and keeps broader neural coverage outside the first lane unless a second representation mode is added."
      deliverables: [deliv-wedge-memo, deliv-residual-register]
      acceptance_tests: [test-wedge-consistency, test-residual-explicitness]
      references: [ref-dandi, ref-ajile-memo, ref-ibl, ref-breadth-summary, ref-way-forward]
  deliverables:
    - id: deliv-wedge-memo
      kind: note
      path: proofs/selected_artifacts/<run>/lane_1_wedge_recommendation.md
      description: "Evidence-bound Lane 1 wedge recommendation"
      must_contain: [recommended wedge, non-recommended scope, authority basis, explicit no-readiness clause]
    - id: deliv-residual-register
      kind: json
      path: proofs/selected_artifacts/<run>/lane_1_residual_decision_register.json
      description: "Residual decisions and risks carried forward after the Phase 4 wedge choice"
      must_contain: [open breadth blocker, optional IBL refinement status, second-mode trigger, commercialization boundary]
  references:
    - id: ref-dandi
      kind: prior_artifact
      locator: proofs/selected_artifacts/2026-03-20_zpe_neuro_repo_realignment/public_corpus_eval_dandi_000034_mouse412804_ecephys.json
      role: positive_anchor
      why_it_matters: "Defines the one public authority anchor that must not be diluted or overstated"
      applies_to: [claim-lane-decision]
      must_surface: true
      required_actions: [read, compare]
    - id: ref-ajile-memo
      kind: prior_artifact
      locator: proofs/selected_artifacts/2026-03-21_zpe_neuro_breadth_adjudication/ajile12_family_boundary_decision.md
      role: family_boundary
      why_it_matters: "Makes AJILE explicitly out-of-family for Lane 1 and prevents hidden breadth drift"
      applies_to: [claim-lane-decision]
      must_surface: true
      required_actions: [read, use]
    - id: ref-ibl
      kind: prior_artifact
      locator: proofs/selected_artifacts/2026-03-20_zpe_neuro_ibl_waveform_probe/public_corpus_ibl_waveform_eval.json
      role: remaining_breadth_blocker
      why_it_matters: "Keeps the real second extracellular target failure visible so the wedge cannot overclaim breadth"
      applies_to: [claim-lane-decision]
      must_surface: true
      required_actions: [read, compare]
    - id: ref-breadth-summary
      kind: prior_artifact
      locator: proofs/selected_artifacts/2026-03-21_zpe_neuro_breadth_adjudication/public_corpus_summary.json
      role: current_verdict
      why_it_matters: "Carries the fail-forward breadth outcome that Phase 4 must translate into product scope"
      applies_to: [claim-lane-decision]
      must_surface: true
      required_actions: [read, use]
    - id: ref-way-forward
      kind: prior_artifact
      locator: docs/team_packet/07_WAY_FORWARD.md
      role: decision_frame
      why_it_matters: "Defines the evidence-first fork between a narrower extracellular wedge and a broader second-mode lane"
      applies_to: [claim-lane-decision]
      must_surface: true
      required_actions: [read, use]
  acceptance_tests:
    - id: test-wedge-consistency
      subject: claim-lane-decision
      kind: human_review
      procedure: "Compare the wedge memo against the carried-forward DANDI, AJILE, IBL, and breadth-summary artifacts."
      pass_condition: "The recommendation narrows to the supported extracellular wedge, does not claim breadth closure, and preserves the DANDI anchor."
      evidence_required: [deliv-wedge-memo, ref-dandi, ref-ajile-memo, ref-ibl, ref-breadth-summary]
      automation: hybrid
    - id: test-residual-explicitness
      subject: claim-lane-decision
      kind: consistency
      procedure: "Inspect the residual register for the remaining IBL blocker, optional refinement path, second-mode trigger, and commercialization boundary."
      pass_condition: "Residual risks and deferred work are explicit, actionable, and non-marketing."
      evidence_required: [deliv-residual-register, ref-ibl, ref-breadth-summary, ref-way-forward]
      automation: manual
  forbidden_proxies:
    - id: fp-readiness-narrative
      subject: claim-lane-decision
      proxy: "Turning the narrowed wedge into a release-readiness or commercialization-ready claim"
      reason: "Phase 4 only decides the honest first lane; it does not close release or breadth gates."
    - id: fp-dandi-breadth
      subject: claim-lane-decision
      proxy: "Using the DANDI pass to imply general public-corpus breadth"
      reason: "The DANDI anchor is sovereign but singular."
    - id: fp-ibl-buried
      subject: claim-lane-decision
      proxy: "Burying the IBL downstream fail inside optional future work"
      reason: "The remaining breadth blocker must stay explicit in the recommendation."
---

<objective>
Produce the missing Phase 4 wedge decision from the existing authoritative evidence.

Purpose: convert the explicit breadth fail-forward verdict into a precise Lane 1 recommendation and residual decision register.
Output: a wedge memo, a residual register, and updated lane state surfaces with no release-boundary drift.
</objective>
