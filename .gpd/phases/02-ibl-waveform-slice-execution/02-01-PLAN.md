---
phase: 02-ibl-waveform-slice-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/zpe_neuro/ibl_public.py
interactive: false

conventions:
  units: "microvolts, samples, bytes, seconds, hertz"
  metric: "N/A"
  coordinates: "zero-based sample windows with half-open ranges [start, start + sample_limit)"

approximations:
  - name: "Single-chunk feasibility path"
    parameter: "chunk_index, sample_limit, channel_limit"
    validity: "M1-local feasibility work where one bounded AP chunk is enough to prove or disprove waveform access"
    breaks_when: "execution requires whole-file downloads, wide channel counts, or multi-chunk stitching"
    check: "artifact or summary must surface exact byte counts, sample bounds, and escalation criteria"

contract:
  schema_version: 1
  scope:
    question: "What is the smallest honest M1-local IBL raw AP path that reaches real waveform bytes without widening scope to whole-file downloads?"
  claims:
    - id: claim-ibl-local-path
      statement: "A concrete public IBL raw AP path exists that can be reduced to a bounded local chunk workflow on the M1."
      deliverables: [deliv-ibl-path-note]
      acceptance_tests: [test-ibl-local-path]
      references: [ref-ibl-probe, ref-way-forward]
  deliverables:
    - id: deliv-ibl-path-note
      kind: note
      path: .gpd/phases/02-ibl-waveform-slice-execution/02-01-SUMMARY.md
      description: "Phase note recording the chosen IBL object trio, chunk bounds, and local feasibility constraints"
      must_contain: [chosen target path, chunk metrics, byte bounds, local constraints]
  references:
    - id: ref-ibl-probe
      kind: prior_artifact
      locator: proofs/selected_artifacts/2026-03-20_zpe_neuro_repo_realignment/public_corpus_ibl_probe.json
      role: benchmark
      why_it_matters: "Defines the starting point as metadata-only visibility"
      applies_to: [claim-ibl-local-path]
      must_surface: true
      required_actions: [read, compare]
    - id: ref-way-forward
      kind: prior_artifact
      locator: docs/team_packet/07_WAY_FORWARD.md
      role: definition
      why_it_matters: "Requires a real second-target waveform path or an honest measured block"
      applies_to: [claim-ibl-local-path]
      must_surface: true
      required_actions: [read, use]
  acceptance_tests:
    - id: test-ibl-local-path
      subject: claim-ibl-local-path
      kind: existence
      procedure: "Confirm that one concrete public IBL AP target, chunk index, and byte range are identified and bounded for local execution."
      pass_condition: "The chosen target and local feasibility limits are explicit and do not rely on metadata-only access."
      evidence_required: [deliv-ibl-path-note, ref-ibl-probe, ref-way-forward]
      automation: hybrid
  forbidden_proxies:
    - id: fp-ibl-metadata
      subject: claim-ibl-local-path
      proxy: "Treating metadata visibility or auth-only listings as a waveform path"
      reason: "Would substitute a lighter probe for the gate-bearing raw-byte path"
  uncertainty_markers:
    weakest_anchors: ["Whether the bounded chunk path survives package integration without needing a wider download path"]
    disconfirming_observations: ["Only full-file staging works, or the public raw-byte route is not reproducible"]
---

<objective>
Identify and encode the smallest honest local IBL waveform path that keeps the work on the M1.

Purpose: convert the prior metadata-only IBL probe into a concrete chunk-level execution path with measured constraints.
Output: a concrete target specification, chunk-feasibility measurements, and an internal summary that makes the local limits explicit.
</objective>

<execution_context>
@/Users/prinivenpillay/.codex/get-physics-done/workflows/execute-plan.md
@/Users/prinivenpillay/.codex/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/02-ibl-waveform-slice-execution/02-CONTEXT.md
@.gpd/phases/02-ibl-waveform-slice-execution/02-RESEARCH.md
@proofs/selected_artifacts/2026-03-20_zpe_neuro_repo_realignment/public_corpus_ibl_probe.json
@docs/team_packet/07_WAY_FORWARD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Choose one concrete public raw AP target</name>
  <files>src/zpe_neuro/ibl_public.py</files>
  <action>Lock one public IBL AP object trio and the corresponding chunk index that minimize local bytes while remaining honest.</action>
  <verify>Confirm the target reaches real raw-byte objects rather than only metadata endpoints.</verify>
  <done>A concrete target path, chunk index, and byte bounds are fixed in the code or summary.</done>
</task>

<task type="auto">
  <name>Task 2: Measure local feasibility constraints</name>
  <files>src/zpe_neuro/ibl_public.py</files>
  <action>Record the chunk sample count, the materialized byte budget, and the local slice dimensions needed for the first honest attempt.</action>
  <verify>Check that the path avoids whole-file staging and makes escalation triggers explicit.</verify>
  <done>The local-feasibility limits are explicit and audit-ready.</done>
</task>

<task type="auto">
  <name>Task 3: Separate existence evidence from downstream quality</name>
  <files>src/zpe_neuro/ibl_public.py</files>
  <action>Structure the future artifact so waveform execution status stays distinct from any downstream insertion outcome.</action>
  <verify>Inspect the payload shape and make sure it cannot narrate a full pass when only slice execution was proven.</verify>
  <done>The future execution artifact can report waveform existence honestly even when downstream checks fail.</done>
</task>

</tasks>

<verification>
Confirm that the chosen IBL path is a real raw-byte route, that its local byte budget is explicit, and that the plan rejects metadata-only success claims.
</verification>

<success_criteria>
A concrete public IBL AP target is fixed, the bounded local chunk workflow is explicit, and the phase is ready to run the waveform eval harness without scope drift.
</success_criteria>

<output>
After completion, create `.gpd/phases/02-ibl-waveform-slice-execution/02-01-SUMMARY.md`.
</output>
