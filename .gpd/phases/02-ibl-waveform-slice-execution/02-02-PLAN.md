---
phase: 02-ibl-waveform-slice-execution
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - pyproject.toml
  - src/zpe_neuro/ibl_public.py
  - tools/run_ibl_public_waveform_eval.py
  - tests/test_ibl_public.py
interactive: false

conventions:
  units: "microvolts, samples, bytes, seconds, hertz"
  metric: "N/A"
  coordinates: "zero-based sample windows with half-open ranges [start, start + sample_limit)"

approximations:
  - name: "Single-chunk waveform evaluation"
    parameter: "chunk_index, sample_limit, channel_limit"
    validity: "Local existence testing where one bounded AP chunk is sufficient to drive the codec and insertion harness"
    breaks_when: "execution needs chunk stitching, whole-file staging, or materially wider windows"
    check: "artifact must surface waveform_slice_executed separately from downstream evaluation_status"

contract:
  schema_version: 1
  scope:
    question: "Can the chosen M1-local IBL AP path produce a real waveform evaluation artifact without relying on the metadata-only proxy?"
  claims:
    - id: claim-second-target
      statement: "A second in-family public target is evaluated through a real IBL waveform path or is honestly blocked with measured local evidence."
      deliverables: [deliv-second-target]
      acceptance_tests: [test-second-target, test-chunk-trimming]
      references: [ref-ibl-probe, ref-way-forward]
  deliverables:
    - id: deliv-second-target
      kind: report
      path: proofs/selected_artifacts/<run>/public_corpus_ibl_waveform_eval.json
      description: "IBL waveform execution artifact or measured local-feasibility result"
      must_contain: [waveform_slice_executed or measured block, target path, resource notes, evaluation split]
  references:
    - id: ref-ibl-probe
      kind: prior_artifact
      locator: proofs/selected_artifacts/2026-03-20_zpe_neuro_repo_realignment/public_corpus_ibl_probe.json
      role: benchmark
      why_it_matters: "Marks the previous IBL state as metadata-only"
      applies_to: [claim-second-target]
      must_surface: true
      required_actions: [read, compare]
    - id: ref-way-forward
      kind: prior_artifact
      locator: docs/team_packet/07_WAY_FORWARD.md
      role: definition
      why_it_matters: "Requires a real waveform path or a measured local block rather than narrative closure"
      applies_to: [claim-second-target]
      must_surface: true
      required_actions: [read, use]
  acceptance_tests:
    - id: test-second-target
      subject: claim-second-target
      kind: existence
      procedure: "Run the IBL waveform evaluation command locally and inspect whether it records a real waveform slice artifact or a measured local block."
      pass_condition: "A second-target artifact exists and does not count metadata-only access as waveform success."
      evidence_required: [deliv-second-target, ref-ibl-probe, ref-way-forward]
      automation: hybrid
    - id: test-chunk-trimming
      subject: claim-second-target
      kind: consistency
      procedure: "Run the focused unit test for chunk-metadata rebasing and invalid-index handling."
      pass_condition: "The chunk metadata helper rebases offsets and bounds correctly and rejects invalid chunk indices."
      evidence_required: [deliv-second-target]
      automation: automated
  forbidden_proxies:
    - id: fp-ibl-metadata
      subject: claim-second-target
      proxy: "Treating metadata-only access as a waveform pass"
      reason: "Would substitute a lighter probe for the actual gate-bearing execution path"
    - id: fp-waveform-equals-pass
      subject: claim-second-target
      proxy: "Treating waveform existence alone as a full insertion success"
      reason: "Would hide downstream evaluation failures behind the existence milestone"
  uncertainty_markers:
    weakest_anchors: ["Whether the packaged single-chunk path runs cleanly through the existing insertion harness on the M1"]
    disconfirming_observations: ["The local eval only works by widening scope beyond the bounded chunk path"]
---

<objective>
Run the IBL raw AP slice through the existing codec and insertion harness on local hardware.

Purpose: replace the metadata-only IBL state with a real waveform-level artifact or a measured local block.
Output: code support, a focused unit test, an executable CLI, and a repo-local proof artifact.
</objective>

<execution_context>
@/Users/zer0pa-build/.codex/get-physics-done/workflows/execute-plan.md
@/Users/zer0pa-build/.codex/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/02-ibl-waveform-slice-execution/02-CONTEXT.md
@.gpd/phases/02-ibl-waveform-slice-execution/02-RESEARCH.md
@.gpd/phases/02-ibl-waveform-slice-execution/02-01-PLAN.md
@proofs/selected_artifacts/2026-03-20_zpe_neuro_repo_realignment/public_corpus_ibl_probe.json
@docs/team_packet/07_WAY_FORWARD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the local chunk execution path</name>
  <files>src/zpe_neuro/ibl_public.py, pyproject.toml</files>
  <action>Keep the dependency surface minimal, materialize one bounded AP chunk locally, and wire the slice into the existing evaluation harness.</action>
  <verify>Confirm the code reaches real waveform data without requiring a whole-file download or metadata-only shortcuts.</verify>
  <done>The package can open one bounded public IBL AP chunk and build a recording from it.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused automation coverage</name>
  <files>tools/run_ibl_public_waveform_eval.py, tests/test_ibl_public.py</files>
  <action>Add a narrow unit test around chunk metadata rebasing and a CLI entry point that executes the local waveform evaluation path.</action>
  <verify>Run the targeted tests locally and make sure the CLI returns a contract-honest status.</verify>
  <done>The single-chunk path is covered by at least one targeted local test and one executable command.</done>
</task>

<task type="auto">
  <name>Task 3: Produce and inspect the second-target artifact</name>
  <files>proofs/selected_artifacts/&lt;run&gt;/public_corpus_ibl_waveform_eval.json</files>
  <action>Execute the local IBL waveform eval into a dedicated proof root and inspect whether the artifact records waveform existence, resource notes, and downstream outcomes honestly.</action>
  <verify>Check that the artifact exists, records materialized bytes and target keys, and keeps waveform execution distinct from downstream pass/fail outcomes.</verify>
  <done>A repo-local second-target artifact exists or a measured local block is recorded explicitly.</done>
</task>

</tasks>

<verification>
Confirm that the IBL artifact comes from a real waveform slice, not metadata alone, and that its payload keeps existence evidence separate from downstream evaluation status.
</verification>

<success_criteria>
The IBL path runs locally through the waveform harness, a second-target artifact exists under the proof root, and no metadata-only or "waveform equals pass" proxy is used.
</success_criteria>

<output>
After completion, create `.gpd/phases/02-ibl-waveform-slice-execution/02-02-SUMMARY.md`.
</output>
