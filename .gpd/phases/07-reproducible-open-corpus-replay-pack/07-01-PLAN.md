---
phase: 07-reproducible-open-corpus-replay-pack
plan: 01
type: execute
wave: 1
depends_on: [06]
files_modified:
  - Makefile
  - REPRODUCING.md
  - src/zpe_neuro/public_corpus.py
  - tools/create_public_corpus_fixture.py
  - .github/workflows/verify-package.yml
  - tests/fixtures/dandi_000034_mouse412804_ecephys_scan_6000x8.npz
  - tests/test_roundtrip.py
  - tests/test_edge_cases.py
  - tests/test_dandi_offline.py
  - tests/test_scaling.py
  - .gpd/phases/07-reproducible-open-corpus-replay-pack/07-01-PLAN.md
  - .gpd/phases/07-reproducible-open-corpus-replay-pack/07-01-SUMMARY.md
  - .gpd/STATE.md
  - .gpd/state.json
interactive: false

conventions:
  units: "microvolts, samples, seconds, hertz"
  framing: "offline replay hardening; not new authority closure"
  artifact_policy: "committed fixture stays small and reproducible; online public replay remains optional and separately provable"

approximations:
  - name: "Small committed DANDI fixture"
    parameter: "one streamed DANDI `000034` slice using the existing sample/channel limits"
    validity: "Phase 7 needs a reproducible offline replay surface, not a full local mirror of the public dataset"
    breaks_when: "the small fixture is narrated as equivalent to a fresh full-dataset rerun"
    check: "keep fixture provenance explicit and keep online rerun commands documented separately"

contract:
  schema_version: 1
  scope:
    question: "Can the repo ship a real offline replay surface for the public-corpus codec path, with committed fixture, tests, docs, and CI coverage?"
  claims:
    - id: claim-offline-public-fixture
      statement: "The repo contains a committed offline fixture derived from the DANDI replay path, and that fixture is executable by local tests without network access."
      deliverables: [deliv-offline-fixture, deliv-offline-tests]
      acceptance_tests: [test-offline-fixture, test-offline-roundtrip]
      references: [ref-brief, ref-phase7-context, ref-public-runner]
    - id: claim-repro-surface-docs
      statement: "The reproduction commands for offline and optional online replay are explicit, accurate, and exercised by CI."
      deliverables: [deliv-repro-doc, deliv-makefile, deliv-ci-extension]
      acceptance_tests: [test-repro-doc, test-ci-offline-verify]
      references: [ref-brief, ref-phase7-context, ref-ci]
  deliverables:
    - id: deliv-offline-fixture
      kind: fixture
      path: tests/fixtures/dandi_000034_mouse412804_ecephys_scan_6000x8.npz
      description: "Small committed DANDI-derived replay fixture for offline codec validation"
      must_contain: [samples, templates, events, source metadata, selection metadata]
    - id: deliv-offline-tests
      kind: test
      path: tests/{test_roundtrip.py,test_edge_cases.py,test_dandi_offline.py,test_scaling.py}
      description: "Offline tests covering fixture replay, scaling, and codec edge behavior"
      must_contain: [fixture roundtrip, edge rejection, offline metadata checks, scaling checks]
    - id: deliv-repro-doc
      kind: note
      path: REPRODUCING.md
      description: "Offline and optional online replay instructions grounded in the actual repo commands"
      must_contain: [offline verify, fixture refresh, public replay, KiloSort4 status]
    - id: deliv-makefile
      kind: config
      path: Makefile
      description: "Stable command surface for install, offline verify, fixture refresh, and public replay"
      must_contain: [install, offline-verify, fixture, public-benchmark]
    - id: deliv-ci-extension
      kind: config
      path: .github/workflows/verify-package.yml
      description: "Existing package workflow extended to exercise the offline replay surface"
      must_contain: [offline replay job or step, fixture-backed test execution]
  references:
    - id: ref-brief
      kind: prior_artifact
      locator: ../ZPE-Neuro_ACTION_BRIEF.md
      role: user_brief
      why_it_matters: "Defines the requested replay-pack additions, even where the repo now needs a more exact implementation."
      applies_to: [claim-offline-public-fixture, claim-repro-surface-docs]
      must_surface: true
      required_actions: [read, reconcile]
    - id: ref-phase7-context
      kind: prior_artifact
      locator: .gpd/phases/07-reproducible-open-corpus-replay-pack/07-CONTEXT.md
      role: phase_guardrail
      why_it_matters: "Preserves the Phase 7 boundary: smallest honest slice first, no AJILE inflation, no duplicate CI."
      applies_to: [claim-offline-public-fixture, claim-repro-surface-docs]
      must_surface: true
      required_actions: [read, carry_forward]
    - id: ref-public-runner
      kind: code
      locator: tools/run_public_corpus_eval.py
      role: execution_surface
      why_it_matters: "Phase 7 must harden the existing replay path instead of inventing a non-existent runner."
      applies_to: [claim-offline-public-fixture]
      must_surface: true
      required_actions: [read, extend]
    - id: ref-ci
      kind: config
      locator: .github/workflows/verify-package.yml
      role: automation_surface
      why_it_matters: "The repo already has package verification automation and should extend it rather than duplicate it."
      applies_to: [claim-repro-surface-docs]
      must_surface: true
      required_actions: [read, extend]
  acceptance_tests:
    - id: test-offline-fixture
      subject: claim-offline-public-fixture
      kind: artifact
      procedure: "Inspect the committed fixture and verify that it carries enough data to reconstruct the replay recording plus source and selection provenance."
      pass_condition: "The fixture exists in-repo and contains samples, templates, events, and metadata needed by the offline tests."
      evidence_required: [deliv-offline-fixture]
      automation: automated
    - id: test-offline-roundtrip
      subject: claim-offline-public-fixture
      kind: execution
      procedure: "Run the offline replay tests against the committed fixture without network access."
      pass_condition: "The fixture-backed codec roundtrip and metadata tests pass locally."
      evidence_required: [deliv-offline-fixture, deliv-offline-tests]
      automation: automated
    - id: test-repro-doc
      subject: claim-repro-surface-docs
      kind: consistency
      procedure: "Inspect REPRODUCING.md and Makefile to verify that offline and optional online replay commands match the actual repo surfaces."
      pass_condition: "The documented commands exist and the offline verify command is locally executable."
      evidence_required: [deliv-repro-doc, deliv-makefile]
      automation: manual
    - id: test-ci-offline-verify
      subject: claim-repro-surface-docs
      kind: configuration
      procedure: "Inspect the existing workflow and verify that it exercises the offline replay surface."
      pass_condition: "CI explicitly runs the fixture-backed offline replay slice."
      evidence_required: [deliv-ci-extension]
      automation: automated
  forbidden_proxies:
    - id: fp-doc-only-repro
      subject: claim-repro-surface-docs
      proxy: "Adding reproduction docs without a committed fixture or executable offline tests"
      reason: "Phase 7 exists to close a real reproducibility gap, not narrate one closed."
    - id: fp-fixture-is-full-rerun
      subject: claim-offline-public-fixture
      proxy: "Treating the committed fixture as evidence of a fresh full public rerun"
      reason: "The fixture proves offline replayability, not full public benchmark closure."
---

<objective>
Execute the first reproducibility-hardening slice for the public replay surface.

Purpose: commit a small DANDI-derived offline replay fixture, add fixture-backed tests, document the real reproduction commands, and extend existing CI so the offline surface stays alive.
Output: fixture generation/load path, committed fixture, offline tests, stable command surface, CI coverage, and updated phase state.
</objective>
