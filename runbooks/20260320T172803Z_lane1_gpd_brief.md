# Lane 1 GPD Bootstrap Brief

## Lane

ZPE-Neuro Lane 1 inside the authoritative inner repo:

- workspace: `/Users/Zer0pa/ZPE/ZPE Neuro`
- repo: `/Users/Zer0pa/ZPE/ZPE Neuro/ZPE-Neuro`
- current gate: `AM-NEU-01`

## Governing Objective

Find the strongest honest commercial wedge for ZPE-Neuro with exponential performance where the authority surface is real, reproducible, and repo-local. The gate is sovereign. If the evidence says the first lane must narrow to an extracellular spike-oriented codec, accept that. If a broader lane needs a second representation mode, state that explicitly instead of narrating breadth that does not exist.

## Existing Authority Anchors

- repo-local Gate C summary: `proofs/selected_artifacts/2026-03-20_zpe_neuro_repo_realignment/gate_c_summary.json` -> `PASS`
- repo-local Gate D summary: `proofs/selected_artifacts/2026-03-20_zpe_neuro_repo_realignment/gate_d_summary.json` -> `PASS`
- DANDI `000034` public insertion: `proofs/selected_artifacts/2026-03-20_zpe_neuro_repo_realignment/public_corpus_eval_dandi_000034_mouse412804_ecephys.json` -> `PASS`
- AJILE12 public breadth probe: `proofs/selected_artifacts/2026-03-20_zpe_neuro_repo_realignment/public_corpus_eval_ajile12_sub01_ses7_ecephys.json` -> `FAIL`
- IBL public probe: `proofs/selected_artifacts/2026-03-20_zpe_neuro_repo_realignment/public_corpus_ibl_probe.json` -> metadata `PASS`, waveform execution still open
- public corpus summary: `proofs/selected_artifacts/2026-03-20_zpe_neuro_repo_realignment/public_corpus_summary.json` -> `FAIL`
- Comet run: `https://www.comet.com/zer0pa/zpe-neuro/f51f70d143834731816085c053e741f3`

## Near-Term Decision

Answer this truthfully:

`Is the first real product a spike-oriented extracellular codec with narrower but stronger authority, or a broader neural-signal codec that needs a different representation mode?`

## Immediate Deliverables

1. A reproducible window-selection artifact for the public corpus harness.
2. A real IBL waveform slice path, if it can be reduced to an M1-local path without forcing RunPod.
3. A decision memo on whether AJILE12 is in-family or out-of-family for the first-lane codec.
4. A rerun of the public-corpus packet with those changes.

## Constraints

- Keep all new code, evidence, and receipts in the inner repo.
- Use GitHub and Comet as supporting operational surfaces, not as substitutes for repo-local proof files.
- Manage disk space tightly. Delete large temporary outputs that are not needed for reproducibility in this workstream.
- Starting compute is limited to:
  - MacBook Air M1
  - Red Magic 10 Pro+ via ADB
- RunPod is available only after a clear local block exists, and only after explicit user confirmation.
- Do not convert partial or mixed evidence into a pass narrative.
- Do not update public-facing docs as though the gate is closed.

## False Progress To Reject

- Treating the DANDI Tier 1 pass as breadth closure.
- Treating quiet-slice compression wins as evidence of codec engagement.
- Treating metadata-only IBL access as waveform execution.
- Treating AJILE12 failure as a minor bug without first deciding whether it is actually in-family.

## Rethink Conditions

- If informative-window reruns still show AJILE12 is not spike-like on the current codec surface, record the family-boundary decision explicitly.
- If IBL waveform slicing is still not locally viable after a scoped M1 attempt, stop and present measured RunPod escalation criteria instead of improvising a workaround narrative.
- If any change regresses the current DANDI Tier 1 authority anchor, treat that as failure regardless of secondary wins.
